server:
  port: 8010

spring:
  application:
    name: country-server
  main:
    allow-bean-definition-overriding: true
    allow-circular-references: true

hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 10000
